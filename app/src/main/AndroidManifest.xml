<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jk.parkingproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ParkingProject">
        <activity android:name=".SignUpActivity"></activity>
        <activity android:name=".AddNewParking"

            />
        <activity
            android:name=".ParkingListActivity"
            android:label="@string/title_activity_parking_list"
            android:theme="@style/Theme.ParkingProject.NoActionBar"

            />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>